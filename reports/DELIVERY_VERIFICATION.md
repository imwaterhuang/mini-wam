# 2026-09-10 项目交付验证

本次交付完成文档一致性、证据可访问性、复现入口和本地版本归档四项工程收尾。
研究范围仍是单训练种子的探索性对照，不增加训练或使用封存最终测试。

## 实际验证

- 原云端证据包 14 个文件大小及哈希全部与原清单一致，原文件未改写。
- 新补 22 个证据文件有独立清单，包括两模型配置/归一化/验证日志/运行日志，
  反事实原始结果、训练完成审计、执行单元、来源链接和视频解码记录。
- 从 40 条回合记录重算 20 对统计，与原报告、逐回合表一致：成功 2/20 与 5/20，精确双侧 p=0.375。
- 两模型验证日志最低点分别是 10,000、34,000 步，完成日志均有 `completed target step 50000`。
- 六段代表视频全部解码，均为 384×384、10 帧/秒；视频帧数等于记录的实际步数加初始帧。
- 额外开发场景与封存测试的种子、初始状态无重叠；这里只读取清单核对，没有执行测试场景。
- 新增入口的帮助信息、Python 语法、错误检查点拒绝、错误配对/篡改证据拒绝通过。
- 真实验证数据小批上的反事实三条件损失有限，模型参数及缓冲区不变。
- 真实模拟器短闭环视频输出通过；补齐了本地 PyAV 与云端视频后端参数差异。
- 结果表、带 Wilson 区间的比较图、反事实图已成功再生成，比较图已人工检查。
  绘图在临时环境使用 matplotlib 3.11.1，没有更换项目训练环境。
- 架构原图根据最终模型代码校正：固定时间编码、两层前缀编码、未来头两层后的激活及实现状态；
  可编辑原图与导出图片已同步并检查，学习者的模型源码未修改。

常规回归 57 项通过、4 项因默认环境跳过；随后四项多进程测试在允许共享内存的本机环境全部通过。
共 61 个测试用例分别完成验证，没有将跳过计作通过。
测试输出见 [常规回归](validation/20260910-tests.txt) 和[多进程补测](validation/20260910-multiworker-tests.txt)。
统计复核见 [closure-summary.json](validation/closure-summary.json)，
带置信区间图见 [paired_comparison_recomputed.png](validation/paired_comparison_recomputed.png)。

## 交付范围的限制

本次实际运行的是历史证据核对、统计再生成及新入口局部验证；没有用正式权重全量重跑 40 个回合，
没有第二台机器从头重训，也没有新增多训练种子。不能把这些未执行项目写成通过。
原始权重保留在私人 Drive，现有权限没有变化；读者可直接核对仓库内的结果和视频，
正式权重重放需要已有访问权，或由项目所有者另行授权。

## 本地版本

本轮使用 `codex/project-closure-20260910` 分支归档，收尾标签为 `v0.1.0-exploratory`。
该标签代表单种子探索性工程交付，不代表原规格中的三种子完整版已经通过。
版本以本地提交和标签为准；本轮不将尚未发布的本地内容描述成 GitHub 上已发布的版本。
可用 `git show v0.1.0-exploratory --stat` 核对固定内容，
用 `git archive --format=zip v0.1.0-exploratory --output=outputs/mini-wam-v0.1.0-exploratory.zip` 导出。

# Mini-WAM 当前工程规格：ACT 动作策略

更新：2026-09-14。ACT（Action Chunking with Transformers，基于 Transformer 的动作分块）是当前唯一模型目标。
状态以 [PROGRESS.md](PROGRESS.md) 为准，验收与随机场景选点规则见 [EVALUATION.md](EVALUATION.md)。
本文是待实现的当前规格，不是 ACT 已训练完成的报告。

## 1. 本阶段目标与边界

用全部现有 Push-T 示范训练基本 ACT，在随机生成的模拟场景中检查实际控制能力，形成可恢复、可复现的训练与闭环评测记录。

| 项目 | 当前决定 |
|---|---|
| 数据来源 | 现有 `lerobot/pusht_image` 本地数据，206 个示范回合、25,650 帧 |
| 训练范围 | 全部 206 个示范回合，不留出示范验证回合 |
| 验证来源 | Push-T 模拟器随机生成的合法初始场景 |
| 检查点选择 | 随机开发场景池的严格成功数；同分比较平均最终覆盖率，再同分保留较早检查点 |
| 首轮训练 | 种子 0；总步数和批大小在模型检查、短训练及目标设备测速后确定 |
| 推进方式 | 学习者编写核心模型，助手解释、审查、验证与整理工程文档 |
| 当前状态 | 协议已确定；ACT 模型、全量数据清单/统计以及训练/评测入口待实现 |

本阶段不加入 WAM（World Action Model，世界动作模型）、未来预测或视频生成，以及 Diffusion Policy（扩散策略）目标。
不把它们列为后续必做阶段。旧实验的未来辅助分支仅保留为历史记录。

## 2. 全量示范训练

### 2.1 回合清单与数据版本

所有 206 个示范回合都是训练数据来源；原先的 21 个验证回合也进入 ACT 训练。
不创建示范验证数据加载器，不计算用于选点的离线验证损失，也不从这批示范另留测试回合。
记录原数据仓库、实际版本、数据指纹和元数据计数，从数据元信息建立全量唯一回合清单。
所有回合均有资格贡献有效窗口；64 窗口诊断子集不改变正式训练使用全量数据的决定。

ACT 的新清单、统计必须使用独立路径。以下是规划名称，当前文件尚未生成：

| 规划路径 | 内容 |
|---|---|
| `splits/act_all_episodes.json` | 所有训练回合标识、数据指纹、总回合数；无保留的示范验证回合 |
| `artifacts/act/data_audit.json` | 全量数据计数、动作/位置范围、有效窗口数和归一化统计 |
| `runs/act/0/<run>/normalization.json` | 本次运行实际使用的统计副本及来源校验值 |

原 `splits/episodes_seed42.json`、`artifacts/data_audit.json` 与历史配置不覆盖。
现有旧数据加载与训练入口使用旧协议；仅修改本文不能使代码自动切换到全量数据。

### 2.2 归一化与有效数据

位置与动作的均值、标准差基于全部 206 个训练回合的有效原始记录重新计算；计数与有效性规则随审计输出。
统计遍历原始记录，不能先生成重叠窗口再计算统计，避免给中间帧重复加权。
无有效动作标签的末尾行不进入动作统计或动作监督；填充值不进入统计或损失。
图像继续使用所选预训练视觉骨干对应的预处理，而非把位置/动作的统计方式套用到像素。

训练与推理使用同一套新统计，标准差设置下限，反归一化后裁剪到环境合法动作范围。
旧检查点保持原统计；ACT 与旧模型重放时，各自加载对应训练时的统计文件。

## 3. ACT 输入、输出与时间对齐

初版采用当前单帧图像和当前智能体二维位置，预测动作块。以下为项目适配接口：

```text
图像 observation_image:   [B, 3, 96, 96]
位置 agent_position:       [B, 2]
动作 action_chunk:         [B, K, 2]，初版 K = 16
有效位 action_valid_mask:  [B, K]，True 表示有有效动作监督
模型预测:                 [B, K, 2]
```

B 表示批大小，K 表示动作块长度。窗口保留回合标识与局部步数以便追溯。
输入为 `o_t, p_t`，监督为 `a_t, ..., a_(t+K-1)`；`a_t` 对应从 `o_t` 到下一观测的环境动作。
Push-T 状态是智能体的二维位置，动作是二维绝对目标坐标；不把状态误写成机械臂关节角，也不将动作直接当作位移。

每个窗口必须位于同一回合内，尾部不足 K 步使用填充并屏蔽。全量使用回合不代表伪造最后一行之后的动作标签。
当前单帧接口可包含回合起点；若沿用旧的两帧窗口索引作为初期适配，须显式记录其起点限制，不能声称新接口已覆盖所有有效起点。
新模型不读取旧数据接口中的未来图像或未来特征监督。

96×96 分辨率、16 步预测长度和二维状态/动作是本项目起步配置，不是原论文默认设置或性能保证。
分辨率、观测历史或动作长度发生改变时，在正式训练前记录配置差异并复查时间对齐。

## 4. 模型职责

参考官方 ACT 说明和实现建立基本模型，包含空间视觉特征、Transformer 编码器/解码器以及 CVAE（Conditional Variational Autoencoder，条件变分自编码器）训练路径，不能把仅增加注意力层的动作回归头标记为完整 ACT。[1][2][3]

训练时：

```text
当前图像 -> 保留空间网格的视觉骨干 -> 视觉 tokens（表示向量）
当前位置 + 有效专家动作块 -> 变分编码器 -> 潜变量分布 -> 采样 z
视觉 tokens + 当前位置 + z -> Transformer 编解码器 -> K 步动作
预测动作 + 真实动作 + 有效掩码 -> 动作重建损失
潜变量分布 -> 正则项
```

推理时：

```text
当前图像 + 当前位置 + z=0 -> ACT -> K 步绝对目标动作
```

基本视觉骨干使用 ResNet-18（Residual Network，残差网络），保留空间特征而非沿用旧全局池化向量；输入投影、隐藏维度与层数写入独立 ACT 配置。
固定时间位置编码继续保留。训练用变分编码器读取示范动作；推理移除该输入，使用固定 `z=0`。这些结构与推理规则以官方实现为参考。[1][3]

训练目标为有效动作上的 L1 重建损失，加潜变量后验相对于标准正态先验的 KL（Kullback–Leibler）散度正则：[3][4]

```text
L_total = L_action_L1 + beta * L_KL
```

分别记录两个损失。有效动作误差按有效坐标数量归一化；接入第三方掩码时，核对 `True` 代表有效还是填充。
变分编码器也要屏蔽填充动作，避免尾部填充值影响有效潜变量。beta 是待配置的正则权重，不能继承旧的未来辅助损失权重。

初版采用动作队列，预测 16 步、执行前 4 步后重新观测；暂不启用时间集成。
时间集成是 ACT 的可选执行方式，使用时需要每个环境步重新预测；若后续在本阶段启用，单独记录执行设置并重新评估，不与执行 4 步的队列模式混报。[2][4]

## 5. 训练与恢复

顺序为：模型/数据契约审查 → 64 个训练窗口过拟合 → 短训练及中断恢复 → 目标设备测速 → 单种子训练。
64 窗口诊断关闭随机增强，记录动作重建误差起点与终点；它验证学习链路，不验证随机场景泛化。
需要降低正则进行诊断时，单独记录配置，不把该诊断运行写成正式 ACT 结果。

超过 1,000 步的云端训练前，在实际目标设备核对消费字段并实测数据读取、传输、前向/反向计算、端到端耗时和峰值内存。
据此确定批大小、梯度累积、训练总步数、检查点保存间隔和随机场景验证间隔；不把旧 50,000 步机械视为 ACT 已确定预算。
先用种子 0 打通链路，本阶段不把多种子大规模实验作为前置要求。

恢复状态包含模型、优化器、调度器、训练步数、随机状态、采样器已消费位置、配置、全量回合清单指纹和归一化副本。
只在一次参数更新成功后推进采样消费位置；数据预取不代表训练完成。
ACT 潜变量采样及随机场景生成使用可追溯的随机状态。闭环验证不得无意改变训练采样序列或模型训练模式，验证结束恢复训练状态。
中断恢复须比较未中断与恢复后的样本顺序、输出、优化器/调度器和损失，并记录精度与容差。

`last.pt` 是最新恢复点；`best.pt` 是按闭环开发指标选中的模型。
训练日志中的实际完成步数和完成标志是执行证据；配置、计划、旧模型检查点均不能代表 ACT 已训练。

## 6. 随机模拟场景验证与选点

所有验证场景由模拟器随机生成，示范数据全部训练。场景使用固定目标与合法、初始静止的状态分布。
生成时记录随机种子、完整可恢复初始状态、环境版本和场景清单校验值，先生成清单再运行候选模型，不按结果挑选容易场景。

建议的起步规模为：5–10 个场景检查接口，50 个随机开发场景进行检查点比较，选定模型后另生成 100 个新随机场景复核。
保存开发场景是为了重复同一批随机抽样、公平比较检查点，不是从示范中扣除验证数据。
数量与验证频率在训练前登记；需要扩充时生成新版本，并让待比较候选在同一版本上重评。
新复核场景与开发池及已记录的人工调试场景去重；原封存测试文件保持未使用。

初版每个场景最多 300 个环境步，预测 16 步、执行 4 步再观测。
严格成功采用 `coverage > 0.95`，核对模拟器成功标志及终止语义，并报告最终覆盖率和最大覆盖率；平均覆盖率不等于成功率。
每次初始化清空动作队列与模型执行状态。保存逐步动作、位置、覆盖率、成功/终止原因和异常，不能只保留成功视频。

检查点排序：严格成功数更高 → 同分时平均最终覆盖率更高 → 仍相同时训练步数更早。
不使用训练损失或不存在的离线验证损失替代该排序。所有开发指标、候选步数及选择原因写入运行记录。

选定模型与推理配置后再执行新随机场景复核，该批结果不反向用于本轮选点。
若据此修改模型或配置，该批场景成为开发历史，下次复核重新随机生成一批并保存种子。
这是模拟场景泛化验证，不是独立人工示范的离线误差测量。细化检查见 [EVALUATION.md](EVALUATION.md)。

## 7. 文件职责与交付

以下 ACT 路径均是待实现规划，不表示文件存在或命令可运行：

| 规划路径 | 职责 |
|---|---|
| `src/mini_wam/models/act.py` | 学习者实现 ACT 网络和损失 |
| `src/mini_wam/data/act_dataset.py` | 全量回合窗口及 ACT 输入适配 |
| `src/mini_wam/training/act.py` | ACT 训练、完整恢复与闭环选点 |
| `src/mini_wam/evaluation/act.py` | 随机场景闭环执行、队列重置和统计 |
| `configs/act_smoke.yaml`、`configs/act_seed0.yaml` | 诊断与训练配置，引用全量清单和新统计 |
| `scripts/train_act.py`、`scripts/evaluate_act.py` | 训练及随机场景验证入口 |
| `runs/act/0/<run>/` | 配置、环境、统计、检查点与完整日志 |
| `reports/act/<run>/` | 随机场景清单、选点记录、复核结果、曲线和视频 |

现有数据审计、缓存、恢复与场景模块可以复用经过核对的通用部分，旧模型接口不视为已支持 ACT。
新入口实现并通过短运行后，才在 [COLAB.md](COLAB.md) 和 [GETTING_STARTED.md](docs/GETTING_STARTED.md) 写入可执行的 ACT 命令。

交付至少包含：全量清单与统计、模型配置与源码版本、训练/恢复记录、开发场景选点表、新随机场景复核指标、成功/失败代表视频和已验证的复现命令。
当前不预写某个成功率作为已达到结果；数据与训练正确、闭环可运行和随机场景成绩可复现，是第一轮工程验收条件。

## 8. 历史隔离与比较口径

2026-09-10 的旧模型训练使用 185 个示范回合，并在 21 个示范回合上按离线损失选点。
额外 20 场景开发结果是 `action_only` 2/20、`future_aware` 5/20，详见[历史收尾报告](reports/PROJECT_CLOSURE.md)。
本轮 ACT 训练数据增加到 206 回合，同时观测接口和选点方式可能改变，不能把差异归因于单独的架构变化。

不同场景池的成功率不直接作配对比较。若另行评测旧模型作参照，使用同一批新随机场景和相同环境判据，记录各自数据与执行配置差异；本阶段不要求重训旧模型。
单训练种子的随机场景结果不衡量训练随机性，也不外推到真实机械臂。
旧权重、原始证据、数据划分和统计保持不变；历史缺口不升级为 ACT 阶段的额外研究目标。

阶段切换前文档：[旧规格](reports/history/SPEC_before_act_20260914.md)、[旧进度](reports/history/PROGRESS_before_act_20260914.md)、[旧验收](reports/history/EVALUATION_before_act_20260914.md)。

## 9. 架构参考

以下为官方参考，查阅日期 2026-09-14。实际接入时记录所用代码的具体提交与依赖版本；网页当前内容不自动替代本项目锁定环境。
全量训练、随机场景选点及上述小型 Push-T 配置是本项目协议，不是对论文原实验的逐项复现。

1. [LeRobot ACT 概述](https://huggingface.co/docs/lerobot/act)
2. [ACT 作者官方实现](https://github.com/tonyzhaozh/act)
3. [LeRobot ACT 模型实现](https://github.com/huggingface/lerobot/blob/main/src/lerobot/policies/act/modeling_act.py)
4. [LeRobot ACT 配置定义](https://github.com/huggingface/lerobot/blob/main/src/lerobot/policies/act/configuration_act.py)

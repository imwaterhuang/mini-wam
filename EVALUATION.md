# ACT 阶段验收与随机场景验证

更新：2026-09-14。ACT（Action Chunking with Transformers，基于 Transformer 的动作分块）使用全部 206 个示范回合训练，不设置示范验证回合。
当前只有文档协议完成；下列实现和运行检查必须取得新证据后才能勾选。旧实验记录见[历史验收快照](reports/history/EVALUATION_before_act_20260914.md)。

## 1. 全量数据与训练接口

- [x] 确定全部示范用于训练，验证改用随机生成的模拟场景。
- [ ] 新建 ACT 全量回合清单，核对 206 个唯一回合，无遗漏、重复或跨回合窗口。
- [ ] 在全部训练回合的有效原始数据上重算位置与动作归一化统计，不按重叠窗口重复加权；原始图像保持所用视觉骨干对应的预处理。
- [ ] ACT 配置使用新清单和新统计，不复用旧 185/21 划分或旧统计文件。
- [ ] 不创建示范验证数据加载器，不以离线验证损失选择 ACT 检查点。
- [ ] 检查当前图像、二维位置、动作块、有效步掩码及首尾时间对齐；无标签末尾行和填充不计入损失。
- [ ] 推理不依赖示范动作或未来图像，反归一化后的动作符合环境绝对目标坐标语义。

## 2. 模型、过拟合与可恢复训练

- [ ] ACT 的空间视觉特征、Transformer 编解码器和 CVAE（Conditional Variational Autoencoder，条件变分自编码器）训练路径已实现并审查。
- [ ] 固定 64 个训练窗口进行过拟合诊断；带掩码动作重建误差明显下降，记录初值、终值及诊断配置。
- [ ] 单独记录动作重建损失与 KL（Kullback–Leibler）散度，检查损失和梯度均为有限值；不要求带正则的总损失降到零。
- [ ] 推理使用固定潜变量 `z=0`，保存并重载后相同输入的动作输出在预定容差内一致。
- [ ] 短训练和中断恢复验证模型、优化器、调度器、随机状态以及实际消费的采样位置。
- [ ] 目标训练设备完成实际字段检查和数据、传输、计算、端到端吞吐测量；据此确定批大小与训练预算。
- [ ] 种子 0 训练有实际日志、检查点和完成标志；配置中的目标步数不能作为已训练步数。

## 3. 随机生成场景的规则

验证数据直接来自 Push-T 模拟器随机初始化，不从示范中留出任何回合，也不把原验证示范改称测试集。
使用标准固定目标、合法且初始静止的随机状态分布。每次场景生成记录随机种子、物体位置和角度、智能体位置、环境版本以及场景清单校验值。
无效初始化仅按与策略无关的规则重新采样，并记录排除原因；困难但合法的场景必须保留。

建议先用 5–10 个随机场景检查执行接口；用于选点的开发池默认随机生成 50 个场景，在首次比较前保存。
同一轮比较检查点时使用同一批场景，以免把随机场景难度变化误当成模型进步。
后续单独随机生成 100 个新场景复核选定模型，先保存清单再执行，并与本阶段开发池及已记录的人工调试场景去重。
以上数量是当前实验起点，训练前写入配置；扩充场景必须记录新版本，相关候选在同一新版场景池上重评后才重新比较。
原有开发场景与封存最终测试文件保持不变，本阶段不调用原封存测试。

- [ ] 场景生成器、随机种子和合法性规则均有记录，执行失败不被静默丢弃。
- [ ] 闭环使用当前观测，初版预测 16 步、执行 4 步后重新观测，每场景最多 300 个环境步。
- [ ] 严格成功采用 `coverage > 0.95`，核对环境成功标志及终止语义；达到阈值即可结束。
- [ ] 每个场景开始时清空动作队列与模型执行状态，保证上个场景不污染下一个场景。
- [ ] 选点开发池和后续新场景复核分别报告，均注明为随机模拟场景结果。

## 4. 检查点选择

训练开始前登记候选检查点保存间隔、闭环验证间隔和开发场景清单版本。
ACT 的 `best.pt` 按开发池中的严格成功数选择；成功数相同，选平均最终覆盖率更高者；仍相同，保留较早训练步数的检查点。
`last.pt` 仅用于最新状态恢复。训练损失用于优化诊断，不代替闭环选点。
开发场景被用于选点或调参，其结果必须标注为开发结果。

选定检查点与推理配置后，再执行新随机场景复核；该轮结果不反向选择检查点。
若根据复核结果修改模型或配置，这些场景随后归入开发历史，并在下一次报告时使用另外一批新随机场景。

- [ ] 每个候选的训练步数、检查点校验值、场景版本和选点指标均可追溯。
- [ ] 明确记录胜出检查点及比较规则，不以个别成功视频选点。
- [ ] 随机复核前固定模型、统计、动作执行参数和成功判据。

## 5. 交付证据与比较边界

- [ ] 报告成功数/总场景数、成功率、Wilson 95% 区间、最终及最大覆盖率、回报和成功场景完成步数。
- [ ] 保存逐步动作、智能体位置、覆盖率、终止原因及异常记录，保留成功和失败代表视频。
- [ ] 保存训练曲线、选点记录、新场景复核结果、环境与配置、运行时间及峰值内存信息。
- [ ] 说明这是单训练种子结果，不把场景间离散程度当成训练随机性。
- [ ] 与旧模型比较时显式注明：旧模型训练 185 回合，ACT 训练 206 回合，且选点协议已改变。

旧 2/20、5/20 仅为历史开发结果。新模型在不同随机场景上的成绩不能与它们直接相减，归因于 ACT 架构。
若另行重放旧模型作参照，必须在同一批新场景、相同环境与成功判据下执行，并标注数据量、观测和执行配置差异；本轮不要求重训旧模型。
工程验收以数据与执行正确、训练可恢复、随机场景结果可复现为准；控制能力是否改善仍需实测，当前不预写成功率。

# Mini-WAM 当前交接状态

更新：2026-09-14。当前进入 ACT（Action Chunking with Transformers，基于 Transformer 的动作分块）动作策略阶段。
本轮只推进 ACT 的实现、训练、恢复和 Push-T 闭环评测；不加入 WAM（World Action Model，世界动作模型）或 Diffusion Policy（扩散策略）目标。

## 当前决定与真实状态

| 项目 | 当前约定 |
|---|---|
| 训练数据 | 现有 206 个示范回合、25,650 帧全部作为训练数据来源；不留出示范验证回合 |
| 数据统计 | ACT 新建全量回合清单与归一化统计；旧 185/21 划分仅供历史复现 |
| 验证方式 | 在模拟器中随机生成合法初始场景，实际闭环执行并记录成功率与覆盖率 |
| 模型选择 | 在同一批随机生成、记录种子的开发场景上比较检查点，按成功率选点；不用离线验证损失 |
| 实施顺序 | 模型与接口 → 64 窗口过拟合 → 短训练及恢复 → 单种子训练 → 随机场景闭环评测 |
| 当前完成 | 阶段文档及数据、选点、评测协议已更新；历史文档已归档 |
| 尚未完成 | ACT 模型、全量数据适配、训练与评测入口均待实现；训练尚未启动 |

数据全部用于训练，不等于填充位置或无有效动作标签的末尾行也参加损失。
当前实现按 [SPEC.md](SPEC.md) 推进，验收按 [EVALUATION.md](EVALUATION.md) 记录。
模型由学习者本人编写；助手解释、审查和验证。下一步先确定 ACT 的当前图像、二维位置和动作块接口，再逐模块实现。

## 历史已完成内容（2026-09-10）


- 环境、数据审计、回合划分、归一化、时间窗口和掩码有实现及测试。
- action_only 的 64 样本过拟合：损失从 0.423381 到 0.006625，重载一致。
- 两种模型各一个种子的训练均达到 50,000 步，运行完成日志已归档。
- 训练支持模型、优化器、调度器、随机状态和已完成采样位置恢复；预取不推进持久游标。
- 图像缓存和性能修复保留代码、验证及云端测量报告。
- 额外 20 个相同开发场景的配对评估、反事实检查、统计和代表视频已归档。
- 文档、架构、证据索引及独立复现入口已整理；验证范围见[交付验证](reports/DELIVERY_VERIFICATION.md)。

## 历史冻结结果与选择规则

下列两模型使用旧的 185 个训练回合、21 个验证回合。其最低离线验证损失选点规则仅适用于旧实验；不能用旧结果宣称本次全量数据 ACT 的提升完全来自架构。

| 模型 | 训练终点 | 最低离线验证损失选中步数 | 额外配对开发集成功数 |
|---|---:|---:|---:|
| action_only | 50,000 | 10,000 | 2/20 |
| future_aware | 50,000 | 34,000 | 5/20 |

action_only 按动作验证损失，future_aware 按预先定义的联合验证损失选点。开发闭环不参与重新选点。
完整检查点哈希见 [FINAL_REPORT.json](reports/closure/FINAL_REPORT.json)。
McNemar 精确双侧 `p=0.375`；中位最终覆盖率 0.810/0.337，不能声称稳定提升。
未来余弦损失：正确 0.03147、打乱 0.03259、合法反向 0.04411，动作依赖证据有限。

早期 `splits/dev_scenes.json` 是另一组 20 个场景：action_only 为 2/20，随机策略为 0/20，
基线平均最终覆盖率 0.481638。它与本轮额外配对集的 0.555553 不混用。
额外配对清单是 `reports/closure/scenes.json`，不改写早期开发集或封存测试。

future_aware 恢复时曾重跑 25,001–26,000 步，原日志有 51,000 行、50,000 个唯一 step。
完成审计保留重复区间及“相同步数取恢复后较晚记录”的策略，不能将行数当作 51,000 个唯一训练步。

## 当前 ACT 阶段状态

| 阶段 | 状态 |
|---|---|
| A0：范围与文档 | 完成；206 回合全量训练，随机模拟场景验证 |
| A1：ACT 模型与数据接口 | 待实现；全量清单和统计尚未生成 |
| A2：64 窗口过拟合、短训练与恢复 | 待执行 |
| A3：种子 0 的 ACT 训练 | 待启动；先测目标设备吞吐，再确定训练预算 |
| A4：随机场景选点与新场景复核 | 待执行；先生成并保存场景及种子，再运行模型 |
| A5：训练曲线、指标、视频与复现记录 | 待形成 ACT 证据；历史产物不能替代 |

旧实验的完成项与证据缺口见[历史验收快照](reports/history/EVALUATION_before_act_20260914.md)。
历史未来分支的未完成研究不作为本轮 ACT 的前置任务。

## 当前续接规则

1. 先读本文、[当前规格](SPEC.md) 和[当前验收](EVALUATION.md)，再核对正在修改的代码。
2. 只推进 ACT 动作策略；全部 206 个示范回合用于训练，不恢复旧示范验证划分。
3. 保持学习者编写核心模型的分工，一次解释和检查一个模块；文档更新不代表代码已完成。
4. 固定时间位置编码的设计继续保留；推理不读取示范未来动作或未来图像。
5. 验证通过随机模拟场景完成，保存种子和初始状态以便复现；原封存最终测试保持未使用。
6. 超过 1,000 步的云端训练前，核对实际消费字段，并在目标设备实测数据、传输、计算和端到端耗时。
7. 数据路径或 worker 数变化后，验证输入张量及完整状态恢复一致性；ACT 使用独立配置、运行目录和统计文件。
8. 旧脚本、旧网页工作台和旧检查点不能标记为已支持 ACT；现有用户代码修改须保留。

## 历史证据入口

- [阶段切换前进度](reports/history/PROGRESS_before_act_20260914.md)、[旧规格](reports/history/SPEC_before_act_20260914.md)与[旧验收](reports/history/EVALUATION_before_act_20260914.md)：仅供历史复现，不作为当前目标。

- `reports/closure/`：原始包、执行单元、配置/验证日志/完成审计、来源和校验清单。
- `reports/performance/`：缓存迁移、目标环境性能与恢复验证。
- `reports/architecture/`：按最终代码校正的结构图、数据样本。
- `scripts/summarize_closure.py`：不加载模型，校验文件并重算统计。
- `scripts/evaluate_paired_closure.py`：冻结检查点的额外开发集配对重放。
- `scripts/evaluate_counterfactual.py`：保持原批次与掩码协议的反事实重放。

原始权重在 Drive；仓库内的结果、图片与六段视频可直接访问。原 Drive 权限保持不变。

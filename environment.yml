name: mini-wam
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip>=25
  - ffmpeg=8
  - pip:
      # LeRobot 0.6.0 corresponds to upstream commit
      # 30da8e687a6dfc617fcd94afc367ac7071c376ce.
      - "lerobot[pusht]==0.6.0"
      - "gradio==6.26.0"
      - "pytest>=8.4,<10"
